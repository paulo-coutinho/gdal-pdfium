

function(conan_message MESSAGE_OUTPUT)
    if(NOT CONAN_CMAKE_SILENT_OUTPUT)
        message(${ARGV${0}})
    endif()
endfunction()


macro(conan_find_apple_frameworks FRAMEWORKS_FOUND FRAMEWORKS FRAMEWORKS_DIRS)
    if(APPLE)
        foreach(_FRAMEWORK ${FRAMEWORKS})
            # https://cmake.org/pipermail/cmake-developers/2017-August/030199.html
            find_library(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND NAME ${_FRAMEWORK} PATHS ${FRAMEWORKS_DIRS} CMAKE_FIND_ROOT_PATH_BOTH)
            if(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND)
                list(APPEND ${FRAMEWORKS_FOUND} ${CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND})
            else()
                message(FATAL_ERROR "Framework library ${_FRAMEWORK} not found in paths: ${FRAMEWORKS_DIRS}")
            endif()
        endforeach()
    endif()
endmacro()


function(conan_package_library_targets libraries package_libdir deps out_libraries out_libraries_target build_type package_name)
    unset(_CONAN_ACTUAL_TARGETS CACHE)
    unset(_CONAN_FOUND_SYSTEM_LIBS CACHE)
    foreach(_LIBRARY_NAME ${libraries})
        find_library(CONAN_FOUND_LIBRARY NAME ${_LIBRARY_NAME} PATHS ${package_libdir}
                     NO_DEFAULT_PATH NO_CMAKE_FIND_ROOT_PATH)
        if(CONAN_FOUND_LIBRARY)
            conan_message(STATUS "Library ${_LIBRARY_NAME} found ${CONAN_FOUND_LIBRARY}")
            list(APPEND _out_libraries ${CONAN_FOUND_LIBRARY})
            if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
                # Create a micro-target for each lib/a found
                set(_LIB_NAME CONAN_LIB::${package_name}_${_LIBRARY_NAME}${build_type})
                if(NOT TARGET ${_LIB_NAME})
                    # Create a micro-target for each lib/a found
                    add_library(${_LIB_NAME} UNKNOWN IMPORTED)
                    set_target_properties(${_LIB_NAME} PROPERTIES IMPORTED_LOCATION ${CONAN_FOUND_LIBRARY})
                    set(_CONAN_ACTUAL_TARGETS ${_CONAN_ACTUAL_TARGETS} ${_LIB_NAME})
                else()
                    conan_message(STATUS "Skipping already existing target: ${_LIB_NAME}")
                endif()
                list(APPEND _out_libraries_target ${_LIB_NAME})
            endif()
            conan_message(STATUS "Found: ${CONAN_FOUND_LIBRARY}")
        else()
            conan_message(STATUS "Library ${_LIBRARY_NAME} not found in package, might be system one")
            list(APPEND _out_libraries_target ${_LIBRARY_NAME})
            list(APPEND _out_libraries ${_LIBRARY_NAME})
            set(_CONAN_FOUND_SYSTEM_LIBS "${_CONAN_FOUND_SYSTEM_LIBS};${_LIBRARY_NAME}")
        endif()
        unset(CONAN_FOUND_LIBRARY CACHE)
    endforeach()

    if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
        # Add all dependencies to all targets
        string(REPLACE " " ";" deps_list "${deps}")
        foreach(_CONAN_ACTUAL_TARGET ${_CONAN_ACTUAL_TARGETS})
            set_property(TARGET ${_CONAN_ACTUAL_TARGET} PROPERTY INTERFACE_LINK_LIBRARIES "${_CONAN_FOUND_SYSTEM_LIBS};${deps_list}")
        endforeach()
    endif()

    set(${out_libraries} ${_out_libraries} PARENT_SCOPE)
    set(${out_libraries_target} ${_out_libraries_target} PARENT_SCOPE)
endfunction()


include(FindPackageHandleStandardArgs)

conan_message(STATUS "Conan: Using autogenerated FindGeoTIFF.cmake")
# Global approach
set(GeoTIFF_FOUND 1)
set(GeoTIFF_VERSION "1.7.0")

find_package_handle_standard_args(GeoTIFF REQUIRED_VARS
                                  GeoTIFF_VERSION VERSION_VAR GeoTIFF_VERSION)
mark_as_advanced(GeoTIFF_FOUND GeoTIFF_VERSION)


set(GeoTIFF_INCLUDE_DIRS "/Users/paulo/.conan/data/libgeotiff/1.7.0/_/_/package/35d40880212d06ef68ee17c7cf449516b078a579/include")
set(GeoTIFF_INCLUDE_DIR "/Users/paulo/.conan/data/libgeotiff/1.7.0/_/_/package/35d40880212d06ef68ee17c7cf449516b078a579/include")
set(GeoTIFF_INCLUDES "/Users/paulo/.conan/data/libgeotiff/1.7.0/_/_/package/35d40880212d06ef68ee17c7cf449516b078a579/include")
set(GeoTIFF_RES_DIRS )
set(GeoTIFF_DEFINITIONS )
set(GeoTIFF_LINKER_FLAGS_LIST
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,SHARED_LIBRARY>:>"
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,MODULE_LIBRARY>:>"
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,EXECUTABLE>:>"
)
set(GeoTIFF_COMPILE_DEFINITIONS )
set(GeoTIFF_COMPILE_OPTIONS_LIST "" "")
set(GeoTIFF_COMPILE_OPTIONS_C "")
set(GeoTIFF_COMPILE_OPTIONS_CXX "")
set(GeoTIFF_LIBRARIES_TARGETS "") # Will be filled later, if CMake 3
set(GeoTIFF_LIBRARIES "") # Will be filled later
set(GeoTIFF_LIBS "") # Same as GeoTIFF_LIBRARIES
set(GeoTIFF_SYSTEM_LIBS )
set(GeoTIFF_FRAMEWORK_DIRS )
set(GeoTIFF_FRAMEWORKS )
set(GeoTIFF_FRAMEWORKS_FOUND "") # Will be filled later
set(GeoTIFF_BUILD_MODULES_PATHS "/Users/paulo/.conan/data/libgeotiff/1.7.0/_/_/package/35d40880212d06ef68ee17c7cf449516b078a579/lib/cmake/conan-official-libgeotiff-variables.cmake")

conan_find_apple_frameworks(GeoTIFF_FRAMEWORKS_FOUND "${GeoTIFF_FRAMEWORKS}" "${GeoTIFF_FRAMEWORK_DIRS}")

mark_as_advanced(GeoTIFF_INCLUDE_DIRS
                 GeoTIFF_INCLUDE_DIR
                 GeoTIFF_INCLUDES
                 GeoTIFF_DEFINITIONS
                 GeoTIFF_LINKER_FLAGS_LIST
                 GeoTIFF_COMPILE_DEFINITIONS
                 GeoTIFF_COMPILE_OPTIONS_LIST
                 GeoTIFF_LIBRARIES
                 GeoTIFF_LIBS
                 GeoTIFF_LIBRARIES_TARGETS)

# Find the real .lib/.a and add them to GeoTIFF_LIBS and GeoTIFF_LIBRARY_LIST
set(GeoTIFF_LIBRARY_LIST geotiff)
set(GeoTIFF_LIB_DIRS "/Users/paulo/.conan/data/libgeotiff/1.7.0/_/_/package/35d40880212d06ef68ee17c7cf449516b078a579/lib")

# Gather all the libraries that should be linked to the targets (do not touch existing variables):
set(_GeoTIFF_DEPENDENCIES "${GeoTIFF_FRAMEWORKS_FOUND} ${GeoTIFF_SYSTEM_LIBS} TIFF::TIFF;PROJ::PROJ")

conan_package_library_targets("${GeoTIFF_LIBRARY_LIST}"  # libraries
                              "${GeoTIFF_LIB_DIRS}"      # package_libdir
                              "${_GeoTIFF_DEPENDENCIES}"  # deps
                              GeoTIFF_LIBRARIES            # out_libraries
                              GeoTIFF_LIBRARIES_TARGETS    # out_libraries_targets
                              ""                          # build_type
                              "GeoTIFF")                                      # package_name

set(GeoTIFF_LIBS ${GeoTIFF_LIBRARIES})

foreach(_FRAMEWORK ${GeoTIFF_FRAMEWORKS_FOUND})
    list(APPEND GeoTIFF_LIBRARIES_TARGETS ${_FRAMEWORK})
    list(APPEND GeoTIFF_LIBRARIES ${_FRAMEWORK})
endforeach()

foreach(_SYSTEM_LIB ${GeoTIFF_SYSTEM_LIBS})
    list(APPEND GeoTIFF_LIBRARIES_TARGETS ${_SYSTEM_LIB})
    list(APPEND GeoTIFF_LIBRARIES ${_SYSTEM_LIB})
endforeach()

# We need to add our requirements too
set(GeoTIFF_LIBRARIES_TARGETS "${GeoTIFF_LIBRARIES_TARGETS};TIFF::TIFF;PROJ::PROJ")
set(GeoTIFF_LIBRARIES "${GeoTIFF_LIBRARIES};TIFF::TIFF;PROJ::PROJ")

set(CMAKE_MODULE_PATH "/Users/paulo/.conan/data/libgeotiff/1.7.0/_/_/package/35d40880212d06ef68ee17c7cf449516b078a579/"
			"/Users/paulo/.conan/data/libgeotiff/1.7.0/_/_/package/35d40880212d06ef68ee17c7cf449516b078a579/lib/cmake" ${CMAKE_MODULE_PATH})
set(CMAKE_PREFIX_PATH "/Users/paulo/.conan/data/libgeotiff/1.7.0/_/_/package/35d40880212d06ef68ee17c7cf449516b078a579/"
			"/Users/paulo/.conan/data/libgeotiff/1.7.0/_/_/package/35d40880212d06ef68ee17c7cf449516b078a579/lib/cmake" ${CMAKE_PREFIX_PATH})

if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
    # Target approach
    if(NOT TARGET GeoTIFF::GeoTIFF)
        add_library(GeoTIFF::GeoTIFF INTERFACE IMPORTED)
        if(GeoTIFF_INCLUDE_DIRS)
            set_target_properties(GeoTIFF::GeoTIFF PROPERTIES INTERFACE_INCLUDE_DIRECTORIES
                                  "${GeoTIFF_INCLUDE_DIRS}")
        endif()
        set_property(TARGET GeoTIFF::GeoTIFF PROPERTY INTERFACE_LINK_LIBRARIES
                     "${GeoTIFF_LIBRARIES_TARGETS};${GeoTIFF_LINKER_FLAGS_LIST}")
        set_property(TARGET GeoTIFF::GeoTIFF PROPERTY INTERFACE_COMPILE_DEFINITIONS
                     ${GeoTIFF_COMPILE_DEFINITIONS})
        set_property(TARGET GeoTIFF::GeoTIFF PROPERTY INTERFACE_COMPILE_OPTIONS
                     "${GeoTIFF_COMPILE_OPTIONS_LIST}")
        
        # Library dependencies
        include(CMakeFindDependencyMacro)

        if(NOT TIFF_FOUND)
            find_dependency(TIFF REQUIRED)
        else()
            message(STATUS "Dependency TIFF already found")
        endif()


        if(NOT proj_FOUND)
            find_dependency(proj REQUIRED)
        else()
            message(STATUS "Dependency proj already found")
        endif()

    endif()
endif()

foreach(_BUILD_MODULE_PATH ${GeoTIFF_BUILD_MODULES_PATHS})
    include(${_BUILD_MODULE_PATH})
endforeach()
